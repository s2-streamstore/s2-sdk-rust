# This crate has been renamed to [`s2-sdk`](https://crates.io/crates/s2-sdk)

Please update your `Cargo.toml`:

```toml
[dependencies]
s2-sdk = "0.22"
```

And update your imports from `use s2::...` to `use s2_sdk::...`.
